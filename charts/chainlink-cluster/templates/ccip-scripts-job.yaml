{{- if ne .Values.ccip.debug true }}
apiVersion: batch/v1
kind: Job
metadata:
  name: ccip-scripts-deploy
  labels:
    app: ccip-scripts-deployer
  annotations:
    prometheus.io/scrape: 'true'
spec:
  template:
    spec:
      containers:
        - name: ccip-scripts
          image: {{.Values.ccip.ccipScriptsImage}}
          env:
            - name: CONFIG_JSON_PATH
              value: /data/config.json
          command: [ "go", "test", "pilot/crib_test.go", "-v" ]
          volumeMounts:
            - name: config-volume
              mountPath: /data
          securityContext:
            capabilities:
              drop:
                - ALL
            runAsUser: 999
            runAsGroup: 999
            runAsNonRoot: true
      restartPolicy: Never
      volumes:
        - name: config-volume
          configMap:
            name: ccip-scripts-config
  backoffLimit: 1
{{- end }}
